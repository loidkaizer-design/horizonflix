# TMDB proxy tokens — used by BOTH the dev/preview Vite middleware
# (vite.config.ts) and the production server proxy (src/lib/tmdb-proxy.server.ts).
# On Vercel, set these in Project → Settings → Environment Variables instead.
TMDB_API_TOKEN=e55425032d3d0f371fc776f302e7c09b
# Optional backup token, tried on 401/403:
# TMDB_API_TOKEN_FALLBACK=

# Supabase (Lovable Cloud) — enables auth, watchlist, comments, ratings.
# VITE_SUPABASE_URL=https://<project-ref>.supabase.co
# VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
